#ifndef __CONTROL_H
#define __CONTROL_H

#include "main.h"

/* ================= ?????? ================= */
typedef enum {
    MODE_IDLE = 0,      // ??
    MODE_TASK_1,        // ??1: A? -> B? (??)
    MODE_TASK_2,        // ??2: A->B->C->D->A ?? (??+??)
    MODE_TASK_3,        // ??3: A->C->B->D->A ?? (??+??)
    MODE_TASK_4         // ??4: ??TASK_3??
} SystemMode;

/* ================= ???????? ================= */
typedef enum {
    PATH_STRAIGHT,      // ????(???,?????????)
    PATH_ARC_LEFT,      // ?????(?????)
    PATH_ARC_RIGHT      // ?????(?????)
} PathType;

/* ================= ?????? ================= */
typedef struct {
    PathType type;      // ????
    float distance_cm;  // ??????????(??????,???????????)
    float target_speed_rpm; // ???? (RPM)
} PathSegment;

/* ================= ???? ================= */
void Control_Init(void);
void Control_Task(void);            // ????(?? 50-100Hz)
void Control_SetMode(SystemMode mode);
void Control_Stop(void);            // ????
float Get_Travel_Distance(void);    // ???????(cm),????

#endif